import React from 'react';
var SendButton = function (_a) {
    var onClick = _a.onClick;
    return (React.createElement("button", { id: "send-button", onClick: onClick }, "Send"));
};
export default SendButton;
